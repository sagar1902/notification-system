const sendEmail = async (email, message) => {
  // Logic to send an email
  console.log(`Sending email to ${email}: ${message}`);
  // Integrate with actual email service provider here
};

module.exports = sendEmail;
