const sendSMS = async (phoneNumber, message) => {
  // Logic to send an SMS
  console.log(`Sending SMS to ${phoneNumber}: ${message}`);
  // Integrate with actual SMS service provider here
};

module.exports = sendSMS;
